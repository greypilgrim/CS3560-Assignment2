
/**
 *
 * @author jmcgr
 */
public class Driver {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        AdminPanel adminControl = AdminPanel.getInstance();
        
        
        
        
        
        
        
        /*User jamesUser = new User("James");
        User benUser = new User("Ben");

        Follower mikeFollow = new Follower("Mike");
        Follower billFollow = new Follower("Bill");

        jamesUser.addObserver(mikeFollow);
        jamesUser.addObserver(billFollow);
        
        benUser.addObserver(billFollow);

        jamesUser.makePost("Hello, world!");
        benUser.makePost("How are you doing?");

        jamesUser.makePost("Goodbye, world!");
        
        jamesUser.printFollowers();
        
        User billUser = new User("Bill");
        
        billUser.addObserver(mikeFollow);
        
        
        billUser.makePost("Bill's first post!");
        
        jamesUser.printPosts();
        */
    }
    
}
